services:
  db:
    image: postgis/postgis:17-3.4-alpine
    container_name: tfms-postgres
    environment:
      POSTGRES_USER: tfms
      POSTGRES_PASSWORD: tfms
      POSTGRES_DB: tfms
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
