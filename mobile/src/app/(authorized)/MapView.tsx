import { View } from 'react-native'
import { GoogleMaps } from 'expo-maps';

export default function MapView() {
  return (
    <View style={{ flex: 1 }}>
      {/* <GoogleMaps.View /> */}
    </View>
  )
}