# nginx inside the firenet-web container. Traefik strips the /firenet prefix, so
# this serves the SPA from the container root with a client-side-routing fallback.
server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    location / {
        try_files $uri /index.html;
    }
}
